# authentik-ingressroute.yaml
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: authentik
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`authentik.workshop.local`)
      kind: Rule
      services:
        - name: authentik
          port: 80