# values.yaml
ingressRoute:
  dashboard:
    enabled: true
    matchRule: Host(`traefik.workshop.local`)
    entryPoints:
      - web
providers:
  kubernetesGateway:
    enabled: true
gateway:
  listeners:
    web:
      namespacePolicy:
        from: All